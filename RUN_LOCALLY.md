# Как запустить сайт с оплатой на своем ПК

Так как вы решили запускать всё у себя, мы будем использовать ваш компьютер как сервер.

## 1. Запуск Сервера

1.  Откройте терминал (VS Code -> Terminal -> New Terminal).
2.  Перейдите в папку сервера:
    ```bash
    cd payment-server
    ```
3.  Запустите сервер:
    ```bash
    node server.js
    ```
    Вы должны увидеть: `Payment server running on http://localhost:3000`

## 2. Открытие Сайта (ВАЖНО!)

Теперь ваш сайт "живет" на порту 3000.
**Не используйте** Live Server (порт 5500 или 5501) для проверки оплаты, иначе кнопка не сработает (будет ошибка путей).

Открывайте сайт строго по этой ссылке:
👉 **http://localhost:3000/join.html**

## 3. Как это работает
*   Сервер (`node server.js`) теперь раздает **и** ваш сайт (HTML, картинки), **и** обрабатывает оплаты.
*   Кнопка отправляет запрос на тот же сервер, с которого открыт сайт.

Если вы захотите, чтобы сайт был доступен из интернета с вашего ПК, вам понадобятся дополнительные инструменты (например, `ngrok`), но для локальной работы этого достаточно.
